name: Semiannual Repo Review

on:
  schedule:
    - cron: "0 3 1 1,7 *"
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/manual.yml
    with:
      command: run
      mode: semiannual
      scope: all
      kind: rankings
    secrets: inherit
