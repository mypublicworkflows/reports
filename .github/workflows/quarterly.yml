name: Quarterly Repo Review

on:
  schedule:
    - cron: "0 3 1 1,4,7,10 *"
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/manual.yml
    with:
      command: run
      mode: quarterly
      scope: all
      kind: rankings
    secrets: inherit
